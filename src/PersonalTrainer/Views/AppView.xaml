<UserControl x:Class="Figroll.PersonalTrainer.Views.AppView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ContentControl Grid.Row="0" x:Name="ActiveItem" />
        <TextBlock Grid.Row="0" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Margin="15,7" Background="Transparent" Foreground="LawnGreen" Text="{Binding ErrorText}" />
        <Button Grid.Row="1" Name="UserAction" IsDefault="True" Content="{Binding ActionText}" />
    </Grid>
</UserControl>