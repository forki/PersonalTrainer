<UserControl x:Class="Figroll.PersonalTrainer.Views.ControllerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <!--               Margin="0, -50"-->
        <Image Grid.Row="0" Name="Image" Source="{Binding BackgroundImage}"
               Panel.ZIndex="0"
               Opacity=".1"
               Margin="0"
               VerticalAlignment="Stretch"
               HorizontalAlignment="Stretch" />
        <ListBox x:Name="Scripts" Grid.Row="0" Panel.ZIndex="1" Margin="25" Opacity="0.6" SelectedItem="{Binding Path=SelectedScript}" SelectionMode="Single">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding ScriptName}" />
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</UserControl>